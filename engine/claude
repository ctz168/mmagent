#!/bin/sh
# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Set up Node.js options with process title maintenance
export NODE_OPTIONS="${NODE_OPTIONS:-} -r ${SCRIPT_DIR}/_set_title.js"

# Execute the agent runner from the same directory
exec "${SCRIPT_DIR}/agent-runner" "$@"
